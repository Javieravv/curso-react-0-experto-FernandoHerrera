// Operador condicional ternario.

const activo = true

// De la manera normal.

const mensaje = ( activo ) ? 'Activo' : 'Inactivo'

// Esta es otra forma, para no usar un else, cuando no sea necesario.

const mensaje1 =  activo  && 'Activo únicamente'

console.log (mensaje, mensaje1)



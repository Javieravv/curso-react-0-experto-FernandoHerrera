
export const procesoPesado = (iteracciones) => {
    for (let i = 0; i < iteracciones; i++) {
        console.log (`Ahí vamos con la iteracción`)
    }
    return `${iteracciones} Iteracciones realizadas.`
}